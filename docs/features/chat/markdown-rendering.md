# Markdown 渲染

> 渲染 Markdown 消息

## 类别

聊天

## 类型

可开关 & 可配置

## 描述

在聊天消息中渲染 Markdown 格式内容。启用后, 消息中的 Markdown 语法 (如 **粗体**、*斜体*、`代码`、标题、列表、链接等) 将按 Markdown 样式渲染显示, 而非以纯文本展示。

## 使用方法

在「模块设置」中启用后, 点击配置 Markdown 渲染样式。
